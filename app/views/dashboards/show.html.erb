<br />

<div class="booyah-box col-xs-10 col-xs-offset-1">
	  <h1 class="flixter-menu-heading-font">What I'm Learning...</h1>
	  <br />
	  		<% current_user.enrolled_courses.each do |course| %>
	  	<h3 class="text-center">
	  	 	<%= link_to course.title, course_path(course) %> 
	  	</h3>
	  	<br />
	  	    <% if course.image2? %>
	  			<%= image_tag course.image2, class: 'img-responsive' %>
	  		<% else %>
	  			<img  class: 'img-responsive' src="/assets/flixter_show_lg.png" />
	  		<% end %>
	  	<br />
	  		<div class="col-xs-12 col-sm-8">
      			<%= course.description %>
   			</div>
   			<div class="col-xs-12 col-sm-4 text-center">
	  		 <%= link_to "More Info...", course_path(course), class: 'btn btn-lg btn-primary' %>
	  		</div>
	  		<hr class="col-xs-8 col-xs-offset-2">
            <br class="clr" / >

	 <% end %>
	 <br />
	 <h1 class="flixter-menu-heading-font">What I'm Teaching...</h1>
  			<% current_user.courses.each do |course| %>
       <h3 class="text-center">
      		<!-- %= course.title % -->
      		<%= link_to course.title, course_path(course) %>
       </h3>
       <br />
       	<% if course.image2?%>
      		 <%= image_tag course.image2, class: 'img-responsive' %>
      	 <% else %>
	  			<img class='img-responsive' src="/assets/flixter_show_lg.png" />
	  		<% end %>
       <br />
       <div class="col-xs-12 col-sm-8">
      	 <%= course.description %>
      </div>
       <div class="col-xs-12 col-sm-4 text-center">
        <%= link_to "More Info...", course_path(course), class: 'btn btn-lg btn-primary' %>
    </div>
    <hr class="col-xs-8 col-xs-offset-2">
    <br class="clr" / >

  <% end %>





</div>